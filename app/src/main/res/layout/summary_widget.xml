<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/summary_widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/summary_widget_bg"
    android:gravity="center_vertical"
    android:orientation="vertical"
    android:padding="14dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/widget_week_prev"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:background="@drawable/summary_widget_chip_bg"
            android:gravity="center"
            android:text="@string/summary_widget_prev_week"
            android:textColor="#FFFFFF"
            android:textSize="14sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/widget_week"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginHorizontal="8dp"
            android:layout_weight="1"
            android:ellipsize="end"
            android:maxLines="1"
            android:text="Apr 21 - 27"
            android:textColor="#6F6258"
            android:textSize="11sp" />

        <TextView
            android:id="@+id/widget_week_next"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:background="@drawable/summary_widget_chip_bg"
            android:gravity="center"
            android:text="@string/summary_widget_next_week"
            android:textColor="#FFFFFF"
            android:textSize="14sp"
            android:textStyle="bold" />
    </LinearLayout>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="8dp"
        android:layout_weight="1">

        <ImageView
            android:id="@+id/widget_timetable"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:adjustViewBounds="true"
            android:contentDescription="@string/summary_widget_description"
            android:scaleType="fitXY" />

        <TextView
            android:id="@+id/widget_viewport_toggle"
            android:layout_width="26dp"
            android:layout_height="26dp"
            android:layout_gravity="top|end"
            android:layout_marginTop="4dp"
            android:layout_marginEnd="4dp"
            android:background="@drawable/summary_widget_chip_bg"
            android:gravity="center"
            android:text="@string/summary_widget_toggle_viewport"
            android:textColor="#FFFFFF"
            android:textSize="14sp"
            android:textStyle="bold" />
    </FrameLayout>
</LinearLayout>
